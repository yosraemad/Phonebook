#include <string.h>
#include <stdlib.h>

char* v_number(char* phone);
char* v_email(char* email);
char* v_name(char* name);
char* v_address(char* name);
char* v_dob(char* dob);

#ifndef PHONEBOOK_VALIDATION_H
#define PHONEBOOK_VALIDATION_H

#endif //PHONEBOOK_VALIDATION_H
